class FeatureFlags {
  // Toggle this to true to enable the new experimental editor features
  static const bool enableNewEditor = false;

  // Toggle this to true to enable AI-powered suggestions
  static const bool enableAiSuggestions = false;
  
  // Experimental UI layout mode
  static const bool enableExperimentalHomeLayout = false;
}
